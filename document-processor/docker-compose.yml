version: "3.9"

services:
  document-processor:
    build: .
    container_name: document-processor
    env_file: .env
    ports:
      - "8010:8010"
    restart: unless-stopped
    volumes:
      - .:/app
